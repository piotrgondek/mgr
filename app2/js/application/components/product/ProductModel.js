(function(Backbone){
    'use strict';

    Backbone.Components.product.ProductModel = Backbone.Model.extend({

        defaults: {
           defaultPicture: 'sigma01.jpg'
        }
    });
})(Backbone);
